import {StyleSheet} from 'react-native';

const theme = StyleSheet.create({
  screen: {
    marginHorizontal: 20,
  },
  title: {
    fontSize: 30,
    marginVertical: 10,
  },
});

export default theme;
